# Research 

## What is the difference between a DBMS and RDBMS 
`RDBMS (Relational Database Management System) is an advanced type of DBMS that stores data in structured, interrelated tables, supporting multiple users, ACID properties for high consistency, and SQL`

## What is difference between DDL and DML 

### DDL 
**dealts with define the sturcture of the tables**
```sql 
create table Table_Name(Columns_name)
```

### DML 
**deals with data you can insert data or select or update**
```sql 
select Columns_name 
from table_name 
```

## Why is Normalization is important for large system 

`Noramalization sepearted our system in small tables and it's has a efficient when you select data and make it readable and editable  but more of them convert your system to complex and not understandable `